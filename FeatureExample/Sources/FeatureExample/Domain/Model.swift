import Foundation

struct Model {
    let id: String
    let property: String

    init(
        id: String,
        property: String
    ) {
        self.id = id
        self.property = property
    }
}
